__pycache__/
*.py[cod]
.pytest_cache/
.ruff_cache/
.mypy_cache/
.venv/
dist/
build/
*.egg-info/
.DS_Store

# Local manual-test scratch (fixtures + run artifacts)
manual_test/

# Shared across worktrees: real dir lives in the primary worktree, symlinked here.
.scratch
.serena/
.orquestalite/
team.json
*.pyc
*.pyo

# El link privado del dataset y el artefacto descargado nunca se versionan.
examples/maintenance_events/dataset.local.toml
examples/maintenance_events/.cache/

# Run artifacts: iax writes 2 MB of features per trial here.
outputs/
.env
!.env.schema
.tmp/
.scratch/
.worktrees/
.journals/
# CES-107: skills are derived from skills-lock.json, so the manifest is tracked and the
# installed tree is not. .agents/rules/ and .agents/snippets/ ARE tracked -- AGENTS.md and
# CLAUDE.md link to them by path, so a clone that lacks them has a broken standards catalog.
.agents/skills
.superpowers/
